# Requirements for NOAA Fire Data Ingestion Lambda
# Python 3.12

# HTTP requests library (primary dependency)
requests==2.31.0

# AWS SDK (included in Lambda runtime, but listed for local development)
boto3>=1.34.0
botocore>=1.34.0

# For local testing/development
pytest>=7.4.0
pytest-mock>=3.12.0
moto>=4.2.0
